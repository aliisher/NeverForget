import {StyleSheet, Text, View} from 'react-native';
import React from 'react';
import BackGroundImages from '../../Components.js/BackGroundImages';

const Signup = () => {
  return (
    <View>
      <BackGroundImages />
    </View>
  );
};

export default Signup;

const styles = StyleSheet.create({});
